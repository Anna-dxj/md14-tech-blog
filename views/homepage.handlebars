{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2><a href="/post-details/{{post.id}}">{{post.title}}</a></h2>
    <p>{{post.text}}</p>
  </div>
  <div class="col-md-6">
  
  </div>
</div>
{{/each}}